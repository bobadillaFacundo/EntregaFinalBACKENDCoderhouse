<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Carrito</title>
</head>

<body>
    <h1>Listado de Carritos</h1>

    <h2>Carrito ID: {{carts._id}}</h2>

    {{#if carts.products.length}}
    <h3>Productos en el Carrito:</h3>
    <ul>
        {{#each carts.products}}
        <li>
            Producto ID: {{this._id}} <br>
            Cantidad: {{this.cantidad}} <br>
            <button class="btn-delete-products-carts1"
                data-id231={{../carts._id}} data-products1={{this._id._id}}>Eliminar Producto</button>
        </li>
        {{/each}}
    </ul>
    {{else}}
    <p>No hay productos en el carrito.</p>
    {{/if}}
    <button class="btn-delete-carts" data-id32={{carts._id}}>Eliminar Carrito</button>
    <button class="btn-volver">Volver</button>

</body>

</html>


<script src="js/cartsList.js"></script>